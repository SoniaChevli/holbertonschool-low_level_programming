#include <stdio.h>
/**
 *main - print all digiits 0-9
 * Return: all digits. Otherwise 0.
 */
int main(void)
{
	int i = 0;
	while (i <= 9)
	{
		printf("%d", i);
		i++;
	}
	printf("\n");
	return (0);
}
