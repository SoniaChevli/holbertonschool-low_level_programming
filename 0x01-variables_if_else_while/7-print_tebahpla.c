#include <stdio.h>
/**
 * main - print alphabet in reverse
 * Return: alphabet in reverse. Otherwise 0
 */
int main(void)
{
	char n = 'z';

	while (n >= 'a')
	{
		putchar(n);
		n--;
	}
		putchar ('\n');
		return (0);

}
